<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="http://localhost:4000/feed/blog.xml" rel="self" type="application/atom+xml" /><link href="http://localhost:4000/" rel="alternate" type="text/html" /><updated>2026-07-27T00:14:04+01:00</updated><id>http://localhost:4000/feed/blog.xml</id><title type="html">Aditya Verma | Blog</title><subtitle>Chronicling</subtitle><entry><title type="html">How to be a good scientist</title><link href="http://localhost:4000/blog/2026-07-26-how-to-be-a-good-scientist/" rel="alternate" type="text/html" title="How to be a good scientist" /><published>2026-07-26T00:00:00+01:00</published><updated>2026-07-26T00:00:00+01:00</updated><id>http://localhost:4000/blog/How-to-be-a-good-scientist</id><content type="html" xml:base="http://localhost:4000/blog/2026-07-26-how-to-be-a-good-scientist/"><![CDATA[<p>How to get better at mathematical and scientific research?</p>

<ol>
  <li>Read (or devote time to your craft) everyday: slowly, deliberately… learning compounds. This is the fundamental step, if we are not putting in enough time, no amount of tips will help. Read things which actually help building the skills you want, textbooks, research papers, classic papers (Shannon etc); avoid sugary/pop stuff.</li>
  <li>Discuss ideas: with a colleague, with an LLM, talk out your thoughts, clarify things, do it on a walk. This helps with recall, building clarity, communication, and learning.</li>
  <li>Artefacts and Teaching: just plain consumption is not enough, try writing everyday, do proofs, do implementation of almost everything you know, write good blogs, make YouTube videaos.  This helps with recall too; and helps us see our blind spots.</li>
  <li>Reach out: reach out to professors, go to office hours, reach out to researchers, go to conferences, talks, try to build a community. This helps in getting new ideas, and staying at the forefront.</li>
  <li>Go deep: Have a thesis in mind, and with that in mind, keep searching, exploring, going deeper, asking questions, too much breadth might not take you anywhere. Having a general vision will at least take you in one direction; and also provides you a “why” for your research and work.</li>
  <li>Spaced Repetition and Recall: for new topics, learning syntax, for math areas you’re not working on currently spaced repetition tools like Anki might help.</li>
  <li>A small problem every day: Of course, you must have some big problem statements always in your mind; but on a day-to-day basis solve the smaller problems, and as you solve them, you prepare yourself for the bigger ones. These can be any problems from you textbooks, programming problems, puzzles, etc.  Read one problem, and take it with you before you’re about to sleep and mull over it. This helps build thinking and problem solving skills.</li>
</ol>

<hr />
<p>2014-18 : 4 years B.E, foundation, exploration, 
2018-2026: 8 years professional SDE, exploring my interests, travelling, earning money, playing sports, self-development, searching for interests.
2026-2028: M.S in CS, done after the advent of AI
2028-2032:  PhD in CS. (at age 36. This isn’t too late to get a PhD).</p>]]></content><author><name></name></author><category term="books" /><summary type="html"><![CDATA[How to get better at mathematical and scientific research?]]></summary></entry><entry><title type="html">Spec Faithfulness</title><link href="http://localhost:4000/blog/2026-07-22-spec-faithfulness/" rel="alternate" type="text/html" title="Spec Faithfulness" /><published>2026-07-22T00:00:00+01:00</published><updated>2026-07-22T00:00:00+01:00</updated><id>http://localhost:4000/blog/Spec-Faithfulness</id><content type="html" xml:base="http://localhost:4000/blog/2026-07-22-spec-faithfulness/"><![CDATA[<ol>
  <li>RQ1: How do we measure ‘faithfulness’ of a formal spec against human intent? [non human-in-the-loop]</li>
</ol>

<p>The human is definitely the ultimate oracle; however, does a partial or non-human/AI oracles exist that can capture how well a spec captures the intent without human involvement? How do we minimize human-in-the-loop? Especially with improving LLM performance can they act as a substitute for human-in-the-loop approaches?</p>

<ul>
  <li>survey existing methods and what they used.</li>
  <li>critique whether those methods are robust</li>
  <li>propose a method [??]</li>
</ul>

<p>How to test an oracle:
“Take requirements with trusted gold specs, inject controlled unfaithfulness (boundary shifts, quantifier swaps, dropped conjuncts, antecedent weakening, off-by-one in temporal operators), and measure each candidate oracle’s detection rate and false-alarm rate against these known-bad mutants”</p>

<ol>
  <li>RQ2: How do we measure and reduce vagueness and ambiguity in human NL requirements to aid the reliable generation of LLM-assited formal specs.</li>
</ol>

<p>( Alt-RQ3: How can we with human+LLM collaboration, best capture human intent in formal spec? [human-in-the-loop] )</p>

<hr />

<ol>
  <li>RQx: What target formalism (Lean, Dafny, TLA+ etc) is best suited to capture human intent to formal spec. Which shows the best accuracy and reliabily for LLM-assisted formally verified software development?</li>
</ol>

<hr />]]></content><author><name></name></author><category term="books" /><summary type="html"><![CDATA[RQ1: How do we measure ‘faithfulness’ of a formal spec against human intent? [non human-in-the-loop]]]></summary></entry><entry><title type="html">Logarithms</title><link href="http://localhost:4000/blog/2026-07-19-logarithms/" rel="alternate" type="text/html" title="Logarithms" /><published>2026-07-19T11:00:00+01:00</published><updated>2026-07-19T11:00:00+01:00</updated><id>http://localhost:4000/blog/Logarithms</id><content type="html" xml:base="http://localhost:4000/blog/2026-07-19-logarithms/"><![CDATA[<p>in progress</p>]]></content><author><name></name></author><category term="books" /><summary type="html"><![CDATA[in progress]]></summary></entry><entry><title type="html">Public Key (Asymmetric) Encryption</title><link href="http://localhost:4000/blog/2026-07-10-public-key-assymmetric/" rel="alternate" type="text/html" title="Public Key (Asymmetric) Encryption" /><published>2026-07-10T23:00:00+01:00</published><updated>2026-07-10T23:00:00+01:00</updated><id>http://localhost:4000/blog/Public-Key-Assymmetric</id><content type="html" xml:base="http://localhost:4000/blog/2026-07-10-public-key-assymmetric/"><![CDATA[<h2 id="intro---what-is-assymetric-encryption">Intro - What is Assymetric Encryption?</h2>

<p>Why? If we use symmetric encryption, in sending emails, we need a shared key with each person we want
to communicate with (think - why?). So, we won’t be able to send an email to anyone we don’t yet know.</p>

<p>Generating and exchanging new symmetric keys will get tedious as more people want to encrypt messages to you</p>

<ul>
  <li>
    <p>In Assym encryption, anyone can send encrypted messages  to us using just our public key.</p>
  </li>
  <li>
    <p>Only, we can decrypt those messages using our private key.</p>
  </li>
</ul>

<h2 id="functional-behaviour">Functional behaviour</h2>

<ol>
  <li>Generate a public key and a private key using a key-gen algorithm.</li>
  <li>Anyone with public key can encrypt the messages</li>
  <li>Only the owner (with the private) key can decrypt.</li>
  <li><em>Risk</em> : the public key may not belong to who they’re claiming to be. Problem for later. If Alice is sending me an email – how do we know if a public key I am using belongs to Alice?</li>
</ol>

<p><img src="/blog/image-6.png" alt="The input (security parameter) usually translates as &quot;how big do you want your keys to be?&quot; (the bigger, the more secure)" /></p>

<h2 id="performance">Performance</h2>
<ol>
  <li>Asyym is very low performant for emails, its slow, the operations it does are computationalally expensive.</li>
  <li>Symm key encryption uses AND, OR, XOR ops which are fast. In practice, this is 100-1000x faster than asymm encryption</li>
</ol>

<h2 id="how-do-we-use-asymm-encryptionm">How do we use Asymm Encryptionm</h2>
<ol>
  <li>Key Exhcanges</li>
  <li>Hybrid Exchanges</li>
</ol>

<h2 id="key-exchanges">Key Exchanges</h2>
<p>Asymm can be used to perform key exchanges. Mostly via key encapsulation
<img src="/blog/image-7.png" alt="alt text" /></p>

<p><em>Key encapsulation</em>: Bob generates random key, then encrypts that key for Alice using public key of Alice. Only Alice can decrypt that cipher. She will therefore get the random key generated by Bob. After this alice/bob will share a key and will communicate via symmetric key encryption.
Remember, asymm encryption is very slow. So, using asymm just for key exhcange reduces the need to use it so much.</p>

<p>This is done usually via RSA algorithm; although RSA is used less as compares to Elliptic Curve Diffie-Hellman algo (ECDH). ECDH has smaller parameter size, and RSA has been found to have some vulnerabilities.</p>

<p><img src="/blog/image-8.png" alt="alt text" /></p>

<p>^ The ‘encrypted symmetric key’ encrypted via Alice’s public key –&gt; is the encapsulation of the key. The ‘encapsulated key’  gets sent to Alice, and only she can unexcapsulate it.</p>

<h2 id="hybrid-encryption">Hybrid Encryption</h2>

<ul>
  <li>Every invocation of aymm encryption can encrypt plaintexts upto a certain length ceiling.</li>
  <li>This largest possible size is limited by the ‘security parameter’ (what is this param?)</li>
  <li>Current recc 512 ASCII chars (4096 bits)</li>
</ul>

<h3 id="what-can-we-do">What can we do?</h3>
<ul>
  <li>Break message into blocks and encrypt them.. but the problem is that asymm encryp is slow.</li>
  <li>Most applns use <em>hyrbid encryption</em> which implicitly uses key encapsulation and <em>authenticated encryption</em>.. making it faster and safer!</li>
</ul>

<h3 id="defn">Defn:</h3>

<ul>
  <li>Hybird encryption is a combination of symm + asymm crypto primitives:
    <ol>
      <li>Non-symm: key-exchange</li>
      <li>Symm: use authenticated encryption</li>
    </ol>
  </li>
</ul>

<p>It similar to key-encapsulation in key-exchange, but we are also encapsulating data as well.</p>

<p>In Summary “We generate a symmetric key; (asymm) use Alice’s public key to encrypt this key, so only Alice can decrypt it; once alice decrypts it: we then use symmetric key encryption (using the just shared key) to communicate with alice”</p>

<p>Funtionally it looks like asymm encryption:
<img src="/blog/image-9.png" alt="alt text" /></p>

<p><img src="/blog/image-10.png" alt="alt text" /></p>

<h3 id="example-elliptic-curve-integrated-encryption-scheme-ecies">Example: Elliptic Curve Integrated Encryption Scheme (ECIES)</h3>

<p><img src="/blog/image-11.png" alt="alt text" /></p>

<p>ECIES IS  a hybrid encrypotion system.</p>

<h2 id="asymmetric-encryption-rsa">Asymmetric Encryption: RSA</h2>

<p>Take numbers from number 1 to p- 1 ; p is a prime; under multiplication modulo p
<img src="/blog/image-12.png" alt="alt text" /></p>

<ul>
  <li>One of the numbers represents the message we want to send</li>
</ul>

<p><img src="/blog/image-13.png" alt="alt text" /></p>

<p><img src="/blog/image-14.png" alt="alt text" /></p>

<h2 id="issues-and-fixes">issues and fixes</h2>

<ul>
  <li>[first issue] if the numbers that can be encrypted for a case are small; then a attacker can brute forrce and fidn all the (plaintext - ciphertext) pairs</li>
</ul>

<p>then ‘PKCS v1.5’ was introduced that fixes the issue by padding the message with random bytes</p>

<ul>
  <li>
    <p>1998 another issue to break PKCS v1.5’ the “million messages attack” – the mitigations were found, but too hard to implement.
(Bleichenbacker’s attack/adapttive chosen ciphertext) – tampers with the ciphertext.</p>
  </li>
  <li>
    <p>‘RSA-OAEP’ fixed the issues to prevent Bleichenbacker. PKCS V2.0
 it comes with a method to generate key.  RSA-OAEP has a secure padding scheme.</p>
  </li>
  <li>
    <p>OAEP vuln were found – but only if the implementation is impoper.  Theoretically its ok.
 Better to have simpler messages</p>
  </li>
</ul>

<h3 id="how-it-works">how it works</h3>
<ol>
  <li>first we pad the message</li>
  <li>paded message is XORs with a random number (masking)</li>
  <li>a tag is added</li>
  <li>these MGFs: are hasing functions / extendable output functions</li>
</ol>

<p>More info
<img src="/blog/image-15.png" alt="alt text" /></p>

<h2 id="rsa-kem">RSA-KEM</h2>

<ul>
  <li>Key encapsulating mech</li>
  <li>Ciphertext: a random number is used to produce a symm key (via hashing). this symm key is used to encrypt via authenticated encryotion.</li>
  <li>RSA KEM is rarely used. Most used are PKCS v1.5 or v2.0 standards.</li>
</ul>

<h2 id="hybrid-encryption-with-ecies-elliptic-curve">Hybrid encryption with ECIES (elliptic curve)</h2>

<ul>
  <li>Most widely used.</li>
  <li>ANSI</li>
  <li>NO KEY ENCAPSULATION. Key exhcange is used</li>
  <li>DH version exists, not widely used.
What is the advantage of using ECIES for hybrid encryption?</li>
  <li>
    <p>It uses ECDH for key exchange, which has smaller parameter sizes</p>
  </li>
  <li>
    <p>step 1: bob create ephemeral key pair (pub, private)</p>
  </li>
  <li>with ephenmeral private key–&gt; we  do a key exchange using alice public key</li>
  <li>hence we get a shared symm key</li>
  <li>
    <p>this symm key is used for (to generate cipher) authenticated encryption with alice.</p>
  </li>
  <li>alice combines the public key she receieved (step 1) with her private key to re-generate the symmetric key</li>
  <li>
    <p>now she can decrypt the ciphertext and can read with integrity  + authenticity</p>
  </li>
  <li>AES-GSM, ChaCha,</li>
</ul>

<hr />

<h2 id="rsa">RSA</h2>]]></content><author><name></name></author><category term="books" /><summary type="html"><![CDATA[Intro - What is Assymetric Encryption?]]></summary></entry><entry><title type="html">Notes on GOFAI</title><link href="http://localhost:4000/blog/2026-07-03-gofai/" rel="alternate" type="text/html" title="Notes on GOFAI" /><published>2026-07-03T21:55:00+01:00</published><updated>2026-07-03T21:55:00+01:00</updated><id>http://localhost:4000/blog/GOFAI</id><content type="html" xml:base="http://localhost:4000/blog/2026-07-03-gofai/"><![CDATA[<p>Lecture 1</p>

<p><em>Introduction</em></p>

<ul>
  <li>AI is the study of building models. Models targeted at ‘thinking’, and action and perception.</li>
  <li>We focus on building the right ‘representations’ that support those above models. Having the right representations makes it easy to think about building models.</li>
</ul>

<hr />

<h2 id="lecture-2-goal-trees-and-problem-solving">Lecture 2: Goal Trees and Problem Solving</h2>

<p><em>Symbolic Integration Programs</em></p>

<p>To solve problems we break the problem down into smaller chunks and solve them. [todo find the graph Prof drew]</p>

<ul>
  <li>it encodes a few ‘safe’ transforms. approx 12</li>
  <li>it encodes a few heuristics.</li>
  <li>then the program parses the problem, and applies the above rules on them.</li>
  <li>It constructs a tree, breaking the problem down sub-problems – represented by using AND nodes.</li>
  <li>Sometimes when multiple transforms can be applied, an OR node is used.</li>
  <li>Some heuristics are encoded on problem section: ie on which OR node to choose. For example: choosing hte one with the least nested function composition.</li>
  <li>All leaf nodes under an AND node have to be solved. Only one under an ‘OR’ node need to be solved.</li>
</ul>

<p><em>Generate and Test</em>
Generate and Test is a fundamental and a strong + simple idea in AI.</p>

<p><img src="/blog/image-3.png" alt="alt text" /></p>

<p><img src="/blog/image-4.png" alt="alt text" /></p>

<ul>
  <li>Meta-knowledge (and I think by extension meta-knowledge are important tools).</li>
  <li>“Knowledge about knowledge is where the power is.” For example in classic expert systems (such as SAINT) organizing the knowledge, seeing the patterns in what to encode, and how to encode/represent the problem made the program much more efficient.</li>
</ul>

<p><img src="/blog/image-5.png" alt="alt text" /></p>

<ul>
  <li>“I also expect that your feeling of intelligence of this program (Symbolic Integrator) is diminished.When we understand how something works, its intelligence seems to vanish”</li>
</ul>

<hr />

<h2 id="lecture-3-reasoning-goal-trees-and-rule-based-expert-systems">Lecture 3 ‘Reasoning: Goal Trees and Rule-based expert systems’</h2>

<p>A program can be implemented such that it implements a goal tree.</p>

<p>It can answer ‘why did you do that’ questions based on what comes ‘above’ (parent node) in the goal tree, and it can answer ‘how’ questions by looking at what comes below (child nodes) in the goal tree.</p>]]></content><author><name></name></author><category term="books" /><summary type="html"><![CDATA[Lecture 1]]></summary></entry><entry><title type="html">Automated Debugging</title><link href="http://localhost:4000/blog/2026-06-01-automated-debugging/" rel="alternate" type="text/html" title="Automated Debugging" /><published>2026-06-01T19:59:00+01:00</published><updated>2026-06-01T19:59:00+01:00</updated><id>http://localhost:4000/blog/Automated-Debugging</id><content type="html" xml:base="http://localhost:4000/blog/2026-06-01-automated-debugging/"><![CDATA[<h2 id="debugging-loop">Debugging loop:</h2>

<p>‘generate’ and test\</p>

<p><img src="/blog/image-2.png" alt="alt text" />
Creation of the baseline reproduction scenario; and then iterative hypothesis and test.</p>
<ol>
  <li>
    <p>requires a fixed testable defect reproduction:
Case A - ie a spec/description for the senario when done that causes the defect to show up.
Case B - a spec/description for the scenario where the defect does not show up.</p>
  </li>
  <li>
    <p>The LLM by default shoul be able to keep repeating and experimenting on Case A;  ‘B’ becomes the case.</p>
  </li>
</ol>

<p>This is quite similar to how humans debug: they reproduce the erroneous scenario (Case A). Figure out what the correct scenario would look like. And then come up with reasons on why Case A might be happenin and how to resolve it to Case B, and then try various hypothesis/ideas until B becomes the C.</p>

<h2 id="reasoninggrounding">Reasoning/Grounding</h2>

<p>For the LLM to become aware of the architecte, several approaches can be used:</p>
<ol>
  <li>Specific skill files which ‘teach’ the LLM about the connector.</li>
  <li>A pre-computed typed knowledge graph of the architecture of the plugin, stored in the DB or a relational format.</li>
  <li>Other structured knowledge such as DB tables with useful information.</li>
  <li>Others … ?</li>
</ol>]]></content><author><name></name></author><category term="books" /><summary type="html"><![CDATA[Debugging loop:]]></summary></entry><entry><title type="html">Basis, Dimensions</title><link href="http://localhost:4000/blog/2026-05-25-basis-dimensions/" rel="alternate" type="text/html" title="Basis, Dimensions" /><published>2026-05-25T21:00:00+01:00</published><updated>2026-05-25T21:00:00+01:00</updated><id>http://localhost:4000/blog/Basis-Dimensions</id><content type="html" xml:base="http://localhost:4000/blog/2026-05-25-basis-dimensions/"><![CDATA[<h2 id="basis">Basis</h2>

<p>A basis is a list of vectors in vector space V, that is linearly independent and spans V.</p>

<h2 id="julia-program">Julia Program</h2>

<p>To test manually whether a list is a basis of R^n we need to do two tests:</p>
<ol>
  <li>Check whether they are linearly independent.</li>
  <li>Check their dimension.</li>
</ol>]]></content><author><name></name></author><category term="books" /><summary type="html"><![CDATA[Basis]]></summary></entry><entry><title type="html">Intuition on Linear Transforms, Linear Independence, Matrices, and Null Space</title><link href="http://localhost:4000/blog/2026-05-22-linear-transforms/" rel="alternate" type="text/html" title="Intuition on Linear Transforms, Linear Independence, Matrices, and Null Space" /><published>2026-05-22T13:00:00+01:00</published><updated>2026-05-22T13:00:00+01:00</updated><id>http://localhost:4000/blog/Linear-Transforms</id><content type="html" xml:base="http://localhost:4000/blog/2026-05-22-linear-transforms/"><![CDATA[<h2 id="linear-transforms">Linear Transforms</h2>

<p>A linear transform (or linear map) from V to W is a function T : V -&gt; W satisfying two properties:</p>
<ol>
  <li><strong>Additivity</strong>: T(u + v) = T(u) + T(v)</li>
  <li><strong>Homogeneity</strong>: T(cv) = cT(v), for all c in field F</li>
</ol>

<p>Intuition: any function between two vector spaces that preserves addition and scalar multiplication.</p>

<h2 id="linear-transforms-and-matrices">Linear Transforms and Matrices</h2>

<p>A matrix can be interpreted as a linear transform.</p>

<p>A 2x2 matrix represents where the basis vectors from vector space A would land in vector space B. If in A we have an element x represented as:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x = a1 * [basis_vector_1] + a2 * [basis_vector_2]
</code></pre></div></div>

<p>then applying the transformation T gives us:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>T(x) = a1 * [column 1 of the matrix] + a2 * [column 2 of the matrix]
</code></pre></div></div>

<p><em>Example</em></p>

<p>Consider a linear map $T : \mathbb{R}^2 \to \mathbb{R}^2$ represented by the matrix:</p>

<div class="kdmath">$$
M = \begin{pmatrix} 2 & 1 \\ 0 & 3 \end{pmatrix}
$$</div>

<p>The columns tell us where the standard basis vectors (1 0) and (0 1) land:</p>

<div class="kdmath">$$
T(e_1) = \begin{pmatrix} 2 \\ 0 \end{pmatrix}, \quad T(e_2) = \begin{pmatrix} 1 \\ 3 \end{pmatrix}
$$</div>

<p>Now take a vector $x = 3e_1 + 2e_2 = \begin{pmatrix} 3 \ 2 \end{pmatrix}$. Applying T:</p>

<div class="kdmath">$$
T(x) = 3 \cdot \begin{pmatrix} 2 \\ 0 \end{pmatrix} + 2 \cdot \begin{pmatrix} 1 \\ 3 \end{pmatrix} = \begin{pmatrix} 6 \\ 0 \end{pmatrix} + \begin{pmatrix} 2 \\ 6 \end{pmatrix} = \begin{pmatrix} 8 \\ 6 \end{pmatrix}
$$</div>

<p>The coefficients (3 and 2) stayed the same in the transform. The matrix tells new destinations for each basis vector from the orignal list.</p>

<p>Again, each column of the matrix is where one basis vector lands after the transformation. The matrix just tells us the new destinations.</p>

<p>An m x n matrix represents a linear map from an n-dimensional space to an m-dimensional space. Column j is where the j-th basis vector gets sent.</p>

<p><em>A direct example from Axler</em></p>

<p>linear maps from $\mathbf{F}^n$ to $\mathbf{F}^m$</p>

<p>To generalize, let $m$ and $n$ be positive integers, let $A_{j,k} \in \mathbf{F}$ for each $j = 1, \ldots, m$ and each $k = 1, \ldots, n$, and define a linear map $T \in \mathcal{L}(\mathbf{F}^n, \mathbf{F}^m)$ by:</p>

<div class="kdmath">$$
T(x_1, \ldots, x_n) = (A_{1,1}x_1 + \cdots + A_{1,n}x_n, \;\ldots\;, A_{m,1}x_1 + \cdots + A_{m,n}x_n)
$$</div>

<p>In fact, every linear map from $\mathbf{F}^n$ to $\mathbf{F}^m$ is of this form.</p>

<p><em>Another practical example: Neural Nets</em></p>

<p>In neural networks, a layer computes something like $y = \sigma(Wx + b)$, where $W$ is a weight matrix, $b$ is a bias, and $\sigma$ is an activation function (like ReLU). The $Wx$ part is a linear transform, the weight matrix $W$ (the transform) maps the input (vectors from space n) from one space to another, exactly as above. The bias and activation function break linearity (otherwise stacking layers would collapse into a single linear map, and the network couldn’t learn anything interesting).</p>

<h2 id="null-space-and-linear-independence">Null Space and Linear Independence</h2>

<p>Say we want to figure out whether a list of vectors w1, w2, w3 is linearly independent.</p>

<p>By definition, the vectors are linearly independent if the only solution to:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>a1*w1 + a2*w2 + a3*w3 = 0
</code></pre></div></div>

<p>is a1 = a2 = a3 = 0.</p>

<p>Now here is the trick: we can represent this set of vectors as a ‘linear transform’, and hence as a matrix. We <em>construct</em> a linear map T by placing w1, w2, w3 as the columns of a matrix. From what we know about matrices, this means:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>T([a1, a2, a3]) = a1*w1 + a2*w2 + a3*w3
</code></pre></div></div>

<p>Looking at the right-hand side: that is exactly the linear combination from the independence definition!</p>

<p>So the question “are these vectors linearly independent?” becomes: **which inputs from the initial vector space does T send to the zero vector?
Is it only the zero vector itself?</p>

<p>A linear map moves vectors from one space to another. The <strong>null space</strong> of T is the set of all inputs that get sent to zero:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>null(T) = { v in V : T(v) = 0 }
</code></pre></div></div>

<p>If the null space is just {0} i.e.,   only the zero vector gets mapped to zero, then the only solution to our linear combination equation is the trivial one, and the vectors are linearly independent.</p>

<p>If the null space contains anything else (for example: some nonzero [a1, a2, a3] that T maps to zero) then we have found a nontrivial linear combination that equals zero, and the vectors are linearly dependent.</p>

<p>The list of vectors is not inherently a transform. We <em>chose</em> to build a transform from them because it reframes the independence question into a null space question, which is a standard  technique.</p>

<h2 id="julia-program-to-test-linear-independence-of-any-list-of-vectors">Julia program to test linear independence of any list of vectors</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>using LinearAlgebra

# Linear Independence
# A list of vectors v1, v2, ..., vn is linearly independent if the only
# solution to a1*v1 + a2*v2 + ... + an*vn = 0 is a1 = a2 = ... = an = 0.

# --- Test vectors in R^3 ---
# These should be linearly independent
v1 = [1, 0, 0]
v2 = [0, 1, 0]
v3 = [0, 0, 1]

# These should be linearly dependent (v3 = v1 + v2)
w1 = [1, 2, 3]
w2 = [4, 5, 6]
w3 = [5, 7, 9]

function is_linear_independent( vectors... )
    # build a linear transform
    T = hcat(vectors...)
    nullSpace = nullspace(T)
    if ( iszero(nullSpace) )
        return true
    else
        return false
    end
end

println(is_linear_independent(v1, v2, v3))
println(is_linear_independent(w1, w2, w3))

</code></pre></div></div>

<h2 id="a-simple-julia-program-to-test-if-a-map-between-two-vector-spaces-is-linear-using-numerial-and-symbolic-computation">A simple julia program to test if a map between two vector spaces is linear (using numerial and symbolic computation)</h2>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>using LinearAlgebra
using Random
using Symbolics

# --- Define your maps here ---

# T should take a vector and return a vector
function T(v; b=0, c=0)
    # TODO: implement the map T(x,y,z) = (2x - 4y + 3z + b, 6x + cxyz)
    x = v[1]
    y = v[2]
    z = v[3]
    return [2*x - 4*y + 3*z + b, 6*x + (c * x * y * z)] 
end

# --- Numerical check (random vectors) ---
# Check if a map is linear by testing additivity and homogeneity
function is_linear_numerical(map; num_tests=100, tol=1e-10)
    #  generate random vectors and scalars
    #  test additivity:   map(v1 + v2) ≈ map(v1) + map(v2)
    #  test homogeneity:  map(λ * v) ≈ λ * map(v)    
    for i = 1:num_tests
        randomV1 = [rand(-1000:1000), rand(-1000:1000), rand(-1000:1000)]
        randomV2 = [rand(-1000:1000), rand(-1000:1000), rand(-1000:1000)]
    
        leftCommute = map(randomV1 + randomV2)
        rightCommute = map(randomV1) + map(randomV2)

        #homegeneity:
        lam = rand(-1000:1000)
        leftHomogen = map( lam * randomV1)
        rightHomogen = lam * map(randomV1)

        if ((leftCommute != rightCommute) || (leftHomogen !=rightHomogen))
            return false
        end
    end

    return true
end


map1 =  v -&gt; T(v; b=10, c=10) #should be non-linear
map2 = v -&gt; T(v; b=0, c = 0) # should be linear

#println( is_linear_numerical(map1))
#println( is_linear_numerical(map2))


# --- Symbolic check (proof) ---

# Check if a map is linear by testing additivity and homogeneity
# symbolically. Returns the simplified residuals so you can see
# exactly what breaks linearity.
function is_linear_symbolic(map, n_in)
    #  create symbolic variables for v1, v2, and λ
    #   use @variables x1, x2, ...  or use Symbolics.variable(:x, i)
    #  compute additivity residual:   simplify.(map(v1 + v2) - (map(v1) + map(v2)))
    # compute homogeneity residual:  simplify.(map(λ * v) - λ * map(v))
    #  return whether both residuals are zero, and the residuals themselvesi
    
    @variables x1 x2 x3 y1 y2 y3 lam
    leftCommute  = map( [x1, x2, x3] + [y1, y2,y3])
    rightCommute = map([x1, x2, x3]) + map([y1, y2, y3])
    
    vector1 = [x1, x2, x3]
    homogeneityTest = simplify.(expand.(map(lam * vector1) - (lam* map(vector1))))
    additivityTest = simplify.(leftCommute - rightCommute)
    if (iszero(additivityTest) &amp;&amp; iszero(homogeneityTest)) 
        return true
    end    
    return false
end

map3 =  v -&gt; T(v; b=10, c=10) #should be non-linear
map4 = v -&gt; T(v; b=0, c = 0) # should be linear
println( is_linear_symbolic(map3, 0))
println( is_linear_symbolic(map4, 0 ))

# --- Matrix extraction ---

# Extract the matrix representation of a linear map
# by applying it to standard basis vectors
function extract_matrix(map, n_in, n_out)
    # TODO: build the matrix column by column using basis vectors e1, e2, ...
end
</code></pre></div></div>]]></content><author><name></name></author><category term="books" /><summary type="html"><![CDATA[Linear Transforms]]></summary></entry><entry><title type="html">Sum of Subspaces</title><link href="http://localhost:4000/blog/2026-05-16-sum-of-subspaces/" rel="alternate" type="text/html" title="Sum of Subspaces" /><published>2026-05-16T09:00:00+01:00</published><updated>2026-05-16T09:00:00+01:00</updated><id>http://localhost:4000/blog/Sum-of-Subspaces</id><content type="html" xml:base="http://localhost:4000/blog/2026-05-16-sum-of-subspaces/"><![CDATA[<h2 id="sum-of-subspaces">Sum of Subspaces</h2>

<p>Video link: https://youtu.be/IG-aN3VHr1I?list=PLGAnmvB9m7zOBVCZBUUmSinFV0wEir2Vw</p>]]></content><author><name></name></author><category term="books" /><summary type="html"><![CDATA[Sum of Subspaces]]></summary></entry><entry><title type="html">Quantum Computation: Qbits</title><link href="http://localhost:4000/blog/2026-05-16-quantum-computation-qbits/" rel="alternate" type="text/html" title="Quantum Computation: Qbits" /><published>2026-05-16T09:00:00+01:00</published><updated>2026-05-16T09:00:00+01:00</updated><id>http://localhost:4000/blog/Quantum-Computation-Qbits</id><content type="html" xml:base="http://localhost:4000/blog/2026-05-16-quantum-computation-qbits/"><![CDATA[<h2 id="quantum-computation-qbits">Quantum Computation: Qbits</h2>

<p>A ‘bit’ is a fundamental concept in classical computing. The ‘quantum bit’ or qbit is it’s analog in quantum computation.</p>

<p>It is treated like an abstract mathematical object, which can be realized by physical systems.
By treating qbits as an abtract entity we are able to build a theory of quantum computation without relying on a specific system.</p>

<h4 id="definition">Definition</h4>

<table>
  <tbody>
    <tr>
      <td>A qbit is a mathematical object which has a state that is a linear combination of</td>
      <td>0&gt; and</td>
      <td>1&gt;</td>
    </tr>
  </tbody>
</table>

<p>The state of a qbit can be represented as:</p>

<table>
  <tbody>
    <tr>
      <td>psy</td>
      <td>= \alpha</td>
      <td>0&gt; + \beta</td>
      <td>1&gt;</td>
    </tr>
  </tbody>
</table>

<p>where \alpha and \beta are complex numbers.</p>

<p>A classical bit can be in either 0 or 1, a qbit, however, can exist in a superposition of the two. Although, when we observe it, we see either 0, or 1, with a certain probability.</p>]]></content><author><name></name></author><category term="books" /><summary type="html"><![CDATA[Quantum Computation: Qbits]]></summary></entry></feed>