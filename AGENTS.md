# Codex Project Notes

- In Markdown blog files, prefer compact one-line KaTeX for short formulas, for example `$$ 0'(x) = 0, $$` instead of a three-line display block.
- Use multi-line display math only when it improves readability for longer expressions, aligned derivations, or multi-step equations.
